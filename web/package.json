{
  "name": "silicon-fury",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "capture": "npm run build && node scripts/capture.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "playwright": "^1.54.2",
    "typescript": "~6.0.2",
    "vite": "^8.2.0"
  }
}
